meta {
  name: Delete signer
  type: http
  seq: 5
}

delete {
  url: {{api.url-base}}/document/signer/:id
  body: none
  auth: bearer
}

params:path {
  id: 614
}

auth:bearer {
  token: {{auth.token}}
}
